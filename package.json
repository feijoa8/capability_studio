{
    "name": "capability-studio-monorepo",
    "private": true,
    "scripts": {
      "dev": "concurrently \"npm run dev --prefix apps/landing\" \"npm run dev --prefix apps/web\""
    },
    "devDependencies": {
      "concurrently": "^8.2.2"
    }
  }